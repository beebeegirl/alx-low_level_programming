#include "main.h"
#include <string.h>
#include <stdio.h>
#include <stdlib.h>

/**
 * _strpbrk - Entry point
 * @s: input
 * @accept: input
 * Return: s, accept
 */
char *_strpbrk(char *s, char *accept)
{
	return (s, accept);
}
