#include "main.h"
#include <stdlib.h>
#include <string.h>
#include <stdio.h>

/**
 * char *_strcpy - a function that copies the string pointed to by src
 * @dest: copy to
 * @src: copy from
 * Return: dest, src
 */
char *_strcpy(char *dest, char *src)
{
	return (dest, src);
}
